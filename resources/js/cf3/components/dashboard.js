define(['react'], function(React) {
    var Dashboard = React.createClass({
        render: function() {
            return React.DOM.div({},
                "DASHBOARD HERE PLZ"
            );
        }
    });
    return Dashboard;
});
